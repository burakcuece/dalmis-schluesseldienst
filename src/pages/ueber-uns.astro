---
import AreaSection from '../components/AreaSection.astro';
import FinalCta from '../components/FinalCta.astro';
import OwnerSection from '../components/OwnerSection.astro';
import PageHero from '../components/PageHero.astro';
import PricePromise from '../components/PricePromise.astro';
import ServiceIndex from '../components/ServiceIndex.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import { services } from '../data/services';
import { business } from '../data/site';
import type { Crumb } from '../lib/schema';

const crumbs: Crumb[] = [
  { label: 'Start', path: '' },
  { label: 'Über uns', path: 'ueber-uns/' },
];
---

<BaseLayout
  title={`Über uns: Schlüsseldienst aus ${business.city}`}
  description={`${business.shortName} ist ein mobiler Schlüsseldienst aus ${business.city} für Türöffnung, Autoöffnung, Schloss und Zylinder sowie Sicherheitstechnik.`}
  path="ueber-uns/"
  crumbs={crumbs}
>
  <PageHero
    title={`Mobiler Schlüsseldienst aus ${business.city}`}
    lead={`Hinter ${business.shortName} steht ${business.owner} aus ${business.city}. Mobil unterwegs für Türöffnung, Autoöffnung, Schloss und Zylinder sowie Sicherheitstechnik.`}
    crumbs={crumbs}
  />

  <OwnerSection />
  <PricePromise />
  <AreaSection showMoreLink={false} />

  <section class="section section-light">
    <div class="container">
      <div class="section-heading">
        <h2>Leistungen</h2>
      </div>
      <ServiceIndex services={services} />
    </div>
  </section>

  <FinalCta />
</BaseLayout>
